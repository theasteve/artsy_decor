<div class="container mx-auto items-center flex-wrap pt-4 pb-12" style="min-height: 900px;">
  <div class="w-full container mx-auto flex flex-wrap items-center justify-between mt-0 px-2 py-3">
    <p class="text-3xl text-gray-800 text-center font-bold">
       Community
    </p>
    <%= link_to "Ask ArtsySpace", new_question_path, class: "bg-teal-500 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <div>
    <ul>
      <% @questions.each do |question| %>
        <li class="comments-li my-6">
          <div>
            <%= link_to user_path(question.user) do %>
              <%= user_avatar(question.user) %>
            <% end %>
            <div class="text-base inline-block ml-2">
              <%= link_to question_path(question) do %> 
                <p class="font-mono text-xl text-gray-800 text-center font-bold hover:text-teal-500">
                  <%= question.title %>
                </p>
              <% end %>
            </div>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</div>
